# INC-102 - Authentication Failures After Certificate Expiration

Users were unable to log in to the application.

Authentication-service logs showed repeated token validation failures and HTTP 401 responses.

Investigation identified an expired signing certificate used for validating authentication tokens.

The certificate was renewed after engineer approval and normal authentication behavior resumed.

Key lesson: when widespread login failures coincide with token validation errors, verify certificate expiration and identity provider configuration.