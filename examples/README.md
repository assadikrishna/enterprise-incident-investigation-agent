# ReAct Demonstration

This example demonstrates the manual ReAct reasoning loop using a scripted LLM (`fake_generate()`).

The purpose is to validate the agent architecture before integrating a real language model through OpenRouter.

Run:

```bash
python -m examples.demo_react